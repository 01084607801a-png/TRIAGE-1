# Model 1 설계 문서 — 필요 병원 등급 예측기

> TRIAGE-1의 판단 핵심. "환자 현장정보 → 필요한 병원 등급(조건)"을 예측하고, Model 2(실제 병원 매칭)로 전달한다.
> 작성: 2026-06 | 평가기준 6항목 매핑 포함

---

## 0. 한 줄 정의
**제한된 현장 정보만으로 환자의 '잠복 중증도'를 추정해, 필요한 병원 등급(Tier)을 확률로 출력하는 하이브리드 AI 모듈.**

---

## 1. 문제 정의 & AI 필요성 (평가기준 1·2)

### 진짜 문제
단순 "가까운 열린 병원 찾기"는 AI가 불필요하다 — 구급대원도 현황을 본다.
**어려운 것은: 현장의 제한·노이즈 정보로 '겉보기엔 안정적이나 실제 중증'인 환자(잠복 중증)를 판별하는 것.**

### 규칙만으로는 안 되는 이유 → AI 필요
| 규칙(if-else)의 한계 | AI가 푸는 방식 |
|---|---|
| 생리기준 민감도 ~72% → 중증 28% 놓침 (Kang 2022) | 약한 다요인 신호를 비선형 통합 |
| 하드 컷오프(SBP<90) — SBP 92는 통과 | 보정된 위험 확률로 경계값 표현 |
| 정적, 개선 불가 | 피드백으로 자가 학습 |
| 알려진 패턴만 인코딩 | 비지도로 미지의 위험군 발견 |

> 핵심 주장: "현장정보 → 잠복 중증 → 올바른 목적지"는 **고차원·비선형·학습형 문제**이며, 이것이 AI의 정당한 자리다.

### 대상 사용자 / 시나리오
- 1차: 119 구급대원 (현장 30초 의사결정)
- 2차: 일반인 (행동 기반 GCS 입력으로 접근성 확보)
- 시나리오: 사고 현장 → 환자 상태 입력 → 필요 등급 판정 → 최적 병원 추천

---

## 2. 모듈 구성 (평가기준 2·4)

```
입력 → [1.1 특징공학] → [1.2 지도분류 ⊕ 1.3 비지도표현형] → [1.4 안전 오버라이드] → 등급+확률
                                         ↑
                              [1.5 피드백 학습 루프]
```

| 모듈 | 역할 | 기법 |
|------|------|------|
| **1.1 특징공학** | 현장 입력 + 파생지표: Shock Index(HR/SBP), rSIG, 연령보정 SBP, 다부위 손상수 | 임상 검증 피처 |
| **1.2 지도 분류기** ⭐ | 현장정보 → 3단계 필요등급 확률 (핵심) | XGBoost/RandomForest + 확률보정 |
| **1.3 비지도 표현형** | 환자 군집화 → 규칙이 놓친 위험군 발견 | K-means/GMM + UMAP |
| **1.4 안전 오버라이드** | CDC RED 충족 시 ML과 무관하게 Tier1 강제 (ML은 상향만, 하향 불가) | 규칙 안전장치 |
| **1.5 피드백 루프** | (입력·추천·실제선택·전원·예후) 로깅 → 주기적 재학습 | 지속학습 |

**설계 철학**: 규칙 = 안전 바닥 / AI = 규칙이 못 잡는 잠복 위험 상향 + 학습. ML은 명백한 중증을 하향하지 못한다(의료 안전).

---

## 3. 데이터 & 라벨 (평가기준 3)

### 라벨: ISS 기반 3단계 (순환 탈출)
| Tier | 필요 병원 | 라벨 정의 | 근거 |
|------|----------|----------|------|
| **1** | 권역외상센터 | ISS≥16 **또는** CDC RED 생리 **또는** 핵심 해부(관통 몸통 등) | Baker 1974(ISS), 최윤희 2022(KTDB), Kang 2022 |
| **2** | 권역/지역응급센터 | ISS 9–15 또는 고위험 기전 | 통계연보 |
| **3** | 지역응급기관 | ISS<9, 기준 미충족 | — |

- **ISS는 입력 피처가 아니다** (현장에선 모르는, 병원 정밀진단 후 점수) → 모델은 "현장단서 → 잠복 중증" 예측 = 진짜 task, 순환 아님

### 데이터 출처 & 한계
- 합성 데이터(문헌 보정 생성). KTDB 원자료는 IRB 필요 → 미확보
- 분포·OR을 업로드 논문에서 calibration:
  - 손상 기전·부위 분포: 2024 외상등록체계 통계연보
  - 사망/수술 OR: Kang et al. (2022)
  - 등급별 결과율(수술·수혈·ICU·사망): 최윤희 외 (2022), KTDB 10,865명 실측
- **한계 명시**: 합성이므로 모델은 "지침·문헌의 다요인 근사"를 학습. 전향적 검증은 피드백 루프로.

---

## 4. 학습 방법 (평가기준 2·5)

### 지도학습 (1.2)
- Train/Val/Test = 70/15/15 층화분할 + 10-fold Stratified CV
- 알고리즘 비교: Logistic Regression / Random Forest / XGBoost
- **class_weight=balanced** — undertriage(중증 누락) 최소화 우선
- 확률 보정(isotonic/Platt) → 의미 있는 위험 확률 출력

### 비지도학습 (1.3)
- 동일 피처 표준화 → K-means/GMM 군집
- 각 군집의 임상 프로파일 해석 → "위험 표현형" 명명
- UMAP 2D 시각화 (발표 자료)

### 안전 오버라이드 (1.4)
- CDC RED 기준 충족 → 최종 등급 = max(ML 예측, Tier1)

---

## 5. 성능 검증 (평가기준 5)

### 핵심 지표 = Tier1 민감도(Recall)
> undertriage 비용 ≫ overtriage 비용. "중증을 놓치지 않는 능력"이 최우선.

- **결정적 실험: 규칙 baseline 대비 ML이 놓친 중증을 N% 추가 포착** → AI 필요성을 수치로 증명
- 보조: 혼동행렬, Tier별 ROC/AUC, 보정곡선, SHAP(설명가능성), 군집 분리도

---

## 6. 확장성 & 윤리 (평가기준 6)
- 피드백 루프 → 실데이터 축적 → 전향적 재학습 → 실제 임상 검증
- 윤리: AI는 보조(최종판단=의료진), 설명가능성(XAI) 제공, 데이터 익명화
- 근거: 대한응급의학회 "AI in EM triage 윤리" 등

---

## 부록 — 핵심 참고 문헌
- Baker et al. (1974). The Injury Severity Score.
- Kang et al. (2022). Field Triage Decision Scheme accuracy, BMC Emerg Med.
- 최윤희 외 (2022). 한국형 중증도분류(KTAS)와 ISS 비교, KTDB 10,865명.
- 2024/2022 외상등록체계 통계연보 (중앙응급의료센터).
- 권역외상센터 사망률 개선 / 국가외상체계 성과 연구 (다수).
