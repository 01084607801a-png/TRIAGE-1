# TRIAGE-1 구성서 (쉽게 보는 설명서)

> 이 앱이 **무엇을, 어떻게** 하는지 모듈별로 풀어 쓴 문서입니다.
> 전문용어는 "쉽게 말하면"으로 다시 풀었어요.

---

## 0. 한 줄 요약
구급대원이 **환자 상태를 입력**하면 → **어느 등급 병원이 필요한지 AI가 판단**하고 → **실시간 병원 정보와 비교**해 → **최적 병원을 30초 안에 추천**하는 웹앱.

---

## 1. 한눈에 보는 전체 흐름

```
[환자 입력]  →  [Model 1: 필요 등급 판단]  →  [Model 2: 병원 매칭]  →  [추천 + AI 설명]
 의식·혈압         "이 환자는 권역외상센터        실시간 병원·병상과         "조선대 권역외상센터,
 손상·위치          가 필요하다(Tier 1)"          비교해 점수화              3.6km, 흉부외과 ✓"
```

**비유하면:**
- **Model 1** = 환자를 보고 *"어느 수준 병원이 필요한가"*를 정하는 **분류 담당 의사**
- **Model 2** = 그 조건에 맞는 *실제 병원을 섭외*하는 **병원 코디네이터**

---

## 2. 구성 요소 (모듈별 상세)

### 🖥️ A. 화면 (프론트엔드)
**파일:** `templates/index.html`, `static/script.js`, `static/style.css`

| 부분 | 하는 일 | 쉽게 말하면 |
|------|---------|------------|
| 의식상태(GCS) 선택 | 행동 기준 6단계 버튼 | "숫자 몰라도 '통증에 회피' 같은 행동으로 고름" |
| 활력징후 입력 | 혈압·호흡·맥박 숫자 | 환자 바이탈 입력칸 |
| 손상부위 체크 | 두부·흉부·복부 등 8개 | 어디 다쳤는지 체크 |
| 위치 | GPS 자동 + 지도(핀) | 환자가 지금 어디 있는지 |
| 결과 카드 | 추천 병원 + 병상 + 장비 + AI근거 | 답안지 |
| PWA | 홈화면 추가·오프라인 | "앱처럼 설치되는 웹" |

---

### 🧠 B. 두뇌 1 — Model 1 (필요 등급 판단) ⭐ 핵심 AI
**파일:** `data_generator_v2.py`, `train_model_v2.py`, `models/tier_model.pkl`

**하는 일:** 현장 정보만 보고 *"겉보기엔 멀쩡해도 사실 중증(잠복 중증)"*까지 잡아내 필요 병원 등급(Tier 1/2/3)을 판단.

**왜 AI가 필요한가:** 단순 규칙(혈압<90 등)은 중증의 **28%를 놓침**(Kang 2022). AI는 약한 신호들을 종합해 더 잡아냄.

| 하위 모듈 | 하는 일 | 쉽게 말하면 |
|-----------|---------|------------|
| **1.1 특징공학** | Shock Index(맥박/혈압) 등 파생지표 계산 | "혈압 정상인데 맥박 높으면 위험 → 숨은 신호 계산" |
| **1.2 지도분류 (RandomForest)** | 현장정보 → Tier 확률 예측 | **핵심 AI** — 학습된 모델이 등급 판단 |
| **1.3 비지도 군집** | 비슷한 환자끼리 묶어 위험군 발견 | "사람이 못 본 위험 패턴을 데이터에서 찾음" |
| **1.4 안전 오버라이드** | 명백한 중증은 무조건 Tier 1로 강제 | "AI가 실수로 중증을 낮추지 못하게 안전장치" |
| **1.5 피드백 루프** | 실제 결과를 모아 재학습 | "쓸수록 똑똑해지는 구조" |

**성능:** 3등급 정확도 86% · 규칙 74% → AI 83% (놓침 9%p 감소) · 잠복 중증 34.7% 추가 포착

---

### 🏥 C. 두뇌 2 — Model 2 (병원 매칭)
**파일:** `app.py`의 `match_hospital()`, `calc_hospital_pair_suitability()`

**하는 일:** Model 1이 정한 "필요 등급"을 기준으로, 실시간 병원들을 점수화해 순위를 매김.

**점수 구성 (가중합):**
- 병원 역량(등급) + 전문과 일치 + 거리 + 가용 병상 + 실시간 상태
- 중증일수록 **거리보다 역량**에 가중치를 더 줌

| 안전 규칙 | 내용 |
|-----------|------|
| 전문과 하드필터 | **API 실측일 때만** 제외 (추론값으론 안 거름 → 놓침 방지) |
| 병상 0개 | 제외 X, 소폭 감점만 (소생실서 즉시 처치 가능) |

---

### 🌐 D. 외부 데이터 (API들)
**파일:** `app.py`의 `fetch_nearby_hospitals()` 등

| API | 주는 정보 | 비고 |
|-----|----------|------|
| **NEMC 목록** (`getEgytListInfoInqire`) | 병원 등급·좌표 | 시도(Q0) 필터 |
| **NEMC 실시간 병상** (`getEmrrmRltmUsefulSckbdInfoInqire`) | 가용 병상·CT·MRI | 시도(STAGE1) 필터 |
| **OSRM** | 도로 거리·시간 | (보조) |
| **Nominatim** | 주소 → 좌표 | 주소 검색 |
| **Gemini** | AI 자연어 설명 | 무료, 없으면 규칙 폴백 |

**핵심 기법 — hpid 조인:** 위치/등급 API와 병상 API를 **병원ID(hpid)로 합쳐서** "이 병원은 권역외상센터인데(①) 지금 응급실 22병상 비었다(②)"는 완전한 정보를 만듦.

---

### 💬 E. AI 설명 (XAI)
**파일:** `app.py`의 `generate_explanation()`, `call_gemini()`

**하는 일:** "왜 이 병원인가"를 자연어 3문장으로 생성 (Gemini 무료). 키 없으면 규칙 기반 문장으로 자동 대체.

---

### 🛡️ F. 안정성 장치
**파일:** `utils/validation.py`, `utils/api_client.py`, `utils/cache.py`

| 장치 | 하는 일 |
|------|---------|
| 입력 검증 | 말도 안 되는 값(혈압 999 등) 차단 |
| API 재시도 | 외부 API 일시 오류 시 자동 재시도 |
| TTL 캐시 | 같은 조회 반복 시 빠르게 (메모리 제한 有) |
| Rate Limit | 과도한 요청 차단 |
| 안전 XML 파싱 | defusedxml로 보안 |

---

## 3. 파일 지도 (어느 파일이 뭐 하나)

```
app.py                    ← 서버 본체 (라우트·추천 파이프라인·API 조인·Gemini)
templates/index.html      ← 화면(입력폼·지도·결과)
static/script.js          ← 화면 동작(입력·추천요청·결과 렌더)
static/style.css          ← 디자인(의료 화이트·그린·레드)
static/sw.js, manifest.json ← PWA(설치·오프라인)

data_generator_v2.py      ← 학습 데이터 생성(문헌 보정 합성)
train_model_v2.py         ← Model 1 학습(지도+비지도+undertriage 실험)
models/tier_model.pkl     ← 학습된 Model 1
triage_model.py           ← (보조) SHAP 설명

utils/validation.py       ← 입력 검증
utils/api_client.py       ← API 재시도
utils/cache.py            ← 캐시

render.yaml, Dockerfile   ← 배포 설정
requirements.txt          ← 서버 필요 패키지
presentation_v2.html      ← 발표 자료 (/pitch2)
```

---

## 4. 데이터 & 학습 (어떻게 똑똑해졌나)

1. **데이터:** KTDB 원자료는 IRB 필요 → 못 구함 → **공개 논문·통계연보로 보정한 합성 데이터** 5,000건
2. **라벨:** "필요 등급"을 **ISS(해부학적 중증도)** 기준으로 정의 → ISS는 입력에 안 넣음 → **순환학습 탈출**
3. **학습:** RandomForest(지도) + K-means(비지도), 균형·확률보정
4. **검증:** 규칙 대비 놓침(undertriage) 얼마나 줄였나로 평가
5. **한계 & 보완:** 합성이라 한계 있음 → **피드백 루프로 실데이터 축적해 재학습**

> 정직 포인트: "가짜 정확도 자랑"이 아니라 **"규칙이 놓친 걸 AI가 더 잡고, 쓸수록 정교해지는 설계"**가 핵심.

---

## 5. 배포 (어떻게 웹에 올렸나)

```
로컬 코드 → GitHub(push) → Render(자동 빌드·배포) → https://...onrender.com
```
- `render.yaml`이 설정 자동 인식, `gunicorn`으로 서버 구동, `/health`로 상태 체크
- 환경변수(키)는 Render에 따로 입력 (`.env`는 비공개)
- **한 번 배포로 앱(`/`) + 발표자료(`/pitch2`) 둘 다 서빙**
- push할 때마다 **자동 재배포** → 발표 직전까지 수정 가능

---

## 6. AI를 "어디에" 썼나 (평가 대비 요약)

| 위치 | AI 종류 | 역할 |
|------|---------|------|
| Model 1.2 | 지도학습(RandomForest) | 필요 등급 예측 (핵심) |
| Model 1.3 | 비지도학습(K-means) | 위험 표현형 발견 |
| AI 설명 | LLM(Gemini) | 이송 근거 자연어 생성 |
| Model 1.5 | 지속학습 | 피드백 기반 재학습(설계) |

→ **단순 API 호출이 아니라**, 규칙이 못 푸는 *비선형·잠복 위험* 문제에 AI를 정당하게 사용.
