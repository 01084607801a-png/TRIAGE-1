# ── TRIAGE-1 런타임 의존성 (Render 배포용) ──
# 서버 구동에 실제 필요한 패키지만. 학습/데스크톱용은 requirements-dev.txt 참조.

Flask>=2.0.0
gunicorn>=20.1.0
requests
python-dotenv
defusedxml>=0.7.1
Flask-Limiter>=3.3.0
anthropic>=0.7.0

# ML 모델(RandomForest) 언피클 — 학습 시점 버전과 맞춰 고정
joblib>=1.3.0
numpy>=1.26.0
scikit-learn==1.8.0
